(ns workout-log.components.snippets
  (:require [kioo.om :as k]
            [workout-log.actions :as a]
            [workout-log.util :as u])
  (:require-macros [kioo.om :refer [defsnippet deftemplate]]))
